# Contributing

Run `sh scripts/check.sh` before a change is proposed.

Platform claims need a runnable test or an explicit unsupported result. Keep changes small. Do not add a dependency when the standard library or an existing module is sufficient.

