# Release

The release candidate path is source build, format check, unit tests, README lint, and a manual doctor run on each supported operating system.

Prebuilt native artifacts, npm distribution, Homebrew packaging, signing, attestations, and a public benchmark remain future release work until their checks exist.

