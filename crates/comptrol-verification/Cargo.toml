[package]
name = "comptrol-verification"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Structured independent outcome verification for Comptrol operations"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
