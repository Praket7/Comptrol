[package]
name = "comptrol-popup"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform popup, modal, and permission-dialog classification and dismissal policy"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
