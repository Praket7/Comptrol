[package]
name = "comptrol-adapter-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bounded manifest and RPC types for isolated Comptrol application adapters"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

