[package]
name = "comptrol-software"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "First-class software discovery, installation, update, and uninstall with verified package identity"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
