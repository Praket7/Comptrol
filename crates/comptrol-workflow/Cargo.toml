[package]
name = "comptrol-workflow"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Typed, parameterized, cryptographically fingerprinted Comptrol workflows"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
