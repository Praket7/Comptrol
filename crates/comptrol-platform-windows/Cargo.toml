[package]
name = "comptrol-platform-windows"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Direct Windows UI Automation route for Comptrol"

[dependencies]
serde_json = "1"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = [
    "Win32_Foundation",
    "Win32_System_Com",
    "Win32_UI_Accessibility",
] }
