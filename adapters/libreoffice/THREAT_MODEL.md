# Threat model

UNO is a powerful application interface. The adapter accepts only typed intent payloads, declared paths under an approved resource scope, and short-lived host tokens. It does not accept model-supplied UNO expressions or scripts.

