# Verification contract

Reads are verified from live UNO objects. Writes are read back from the same document model. Save operations additionally verify the target artifact exists and its modified state is clear. Export formats are not advertised until a format-specific artifact verifier is added.

