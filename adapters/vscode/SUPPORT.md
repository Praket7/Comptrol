# Support boundary

Supported only for a user-installed VS Code extension bridge with an authenticated loopback socket. Remote workspaces, arbitrary command execution, terminal input, secret storage, and extension installation are unsupported.

