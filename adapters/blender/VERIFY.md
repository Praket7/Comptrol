# Verification contract

Scene mutations are verified by a follow-up Blender state query. Saves verify the resulting `.blend` artifact exists and can be reopened by Blender. Renders verify the requested output artifact and basic format metadata.

