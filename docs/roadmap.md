# Roadmap

The foundation now includes durable idempotency and reconciliation, a real macOS AX route, exact macOS application launch, opt in Windows and Linux semantic brokers, browser target discovery, bounded accessibility snapshots, event sequencing, bounded waits, checkpoints, trace replay, local pairing state, and a conformance harness.

The current fast verified milestone includes a narrow live browser websocket route for exact target evaluation, navigation, fill, click, bounded waits, accessibility snapshots, history navigation, exact live tab close, sandbox upload and download, target-scoped visual evidence, durable asynchronous tasks, deterministic route planning, verified trace compilation, four isolated application adapter runners, and a direct Windows UI Automation source crate. Generic CDP upload intentionally reports only file selection until an application-specific postcondition verifies transfer acceptance. macOS also has a semantic foreground route for reopening one exact closed Chrome group. Portable closed group restoration, remote mutual TLS transport, full pixel coordinate recovery, direct Linux AT-SPI and macOS AX native crates, live application acceptance, and matched same-task comparison against other computer-use implementations remain open. They are not represented as universal capabilities.
