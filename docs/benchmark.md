# Local benchmark

Run `python3 scripts/benchmark.py` after building the debug binary.

The script measures warm MCP ping latency over the local standard input and output transport. It reports raw local measurements only. It does not claim daemon latency, browser latency, semantic action latency, or cross platform performance.

