# Local dashboard

The loopback HTTP preview serves a read only diagnostics page at `/dashboard`.

It shows the structured doctor result, platform capability state, operation journal location, stop latch state, and browser configuration. The page is local only and does not expose screenshots or remote control.

The dashboard is a diagnostic surface. It is not an authentication boundary for remote access.

