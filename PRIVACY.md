# Privacy

Comptrol does not require network access for normal startup. It does not send screenshots, accessibility text, clipboard content, credentials, URLs, commands, or file contents to a service.

The local audit journal records sanitized operation metadata. Configure a separate state directory when testing. Remove that directory only after confirming it contains no data that you need.

