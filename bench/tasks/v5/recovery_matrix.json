{
  "task_id": "v5.recovery_matrix",
  "benchmark_suite": "recovery",
  "goal": "Verify fault injection: idempotent delivery, latch behavior, intent refusal, consent gating.",
  "timeout_ms": 60000,
  "initial_state": "fresh Comptrol state directory",
  "required_capabilities": ["workflow.execute", "privacy.consent", "recovery.fault_injection"],
  "tasks": [
    {
      "task_id": "v5.recovery.duplicate_delivery",
      "goal": "Submit the same operation with the same idempotency key twice; verify no duplication.",
      "iterations": 2,
      "final_verifier": "response contains executed: true and duplicate_count is 0",
      "steps": [
        {"method": "workflow.execute", "params": {"operation": "test_op", "idempotency_key": "dup-key-1"}}
      ]
    },
    {
      "task_id": "v5.recovery.cancel_latch",
      "goal": "Start a long-running operation and cancel it; verify the latch prevents post-cancel mutations.",
      "iterations": 2,
      "final_verifier": "response contains cancelled: true and no mutations after cancel",
      "steps": [
        {"method": "workflow.execute", "params": {"operation": "long_op", "cancel": true}}
      ]
    },
    {
      "task_id": "v5.recovery.unknown_intent_refusal",
      "goal": "Submit an unknown intent and verify it is refused with an error code.",
      "iterations": 1,
      "final_verifier": "response contains error code indicating unknown_intent",
      "steps": [
        {"method": "workflow.execute", "params": {"intent": "unknown_intent_xyz"}}
      ]
    }
  ],
  "final_verifier": "all task final_verifiers pass"
}