{
  "task_id": "v5.adapter_matrix",
  "benchmark_suite": "adapter",
  "goal": "Verify all 14 adapters respond to handshake, capabilities, and describe-class commands via the public MCP interface.",
  "timeout_ms": 60000,
  "initial_state": "fresh Comptrol state directory",
  "required_capabilities": ["tools/call"],
  "tasks": [
    {
      "task_id": "v5.adapter.vscode_workspace_list",
      "goal": "List VS Code workspaces via the adapter.",
      "iterations": 2,
      "verifier": "route_available_or_unavailable",
      "steps": [
        {"method": "tools/call", "params": {"name": "operate", "arguments": {"intent": "vscode.workspace.list", "idempotency_key": "bench-vscode-ws-001"}}}
      ]
    },
    {
      "task_id": "v5.adapter.resolve_video_timeline_list",
      "goal": "List DaVinci Resolve video timelines via the adapter.",
      "iterations": 2,
      "verifier": "route_available_or_unavailable",
      "steps": [
        {"method": "tools/call", "params": {"name": "operate", "arguments": {"intent": "video.timeline.list", "idempotency_key": "bench-resolve-tl-001"}}}
      ]
    },
    {
      "task_id": "v5.adapter.capabilities_all",
      "goal": "Request capabilities from all adapters and verify non-empty result.",
      "iterations": 1,
      "verifier": "capabilities_non_empty",
      "steps": [
        {"method": "tools/call", "params": {"name": "operate", "arguments": {"intent": "inspect", "params": {"kind": "capabilities"}}}}
      ]
    }
  ],
  "final_verifier": "all task verifiers pass"
}