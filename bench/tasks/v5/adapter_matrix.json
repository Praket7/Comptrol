{
  "task_id": "v5.adapter_matrix",
  "benchmark_suite": "adapter",
  "goal": "Benchmark representative first-party adapter runtime routes after the separate V5 runtime-catalog gate verifies all 14 shipped adapters.",
  "timeout_ms": 60000,
  "initial_state": "fresh Comptrol state directory",
  "required_capabilities": [
    "tools/call"
  ],
  "tasks": [
    {
      "task_id": "v5.adapter.vscode_workspace_list",
      "goal": "Exercise the VS Code read-only workspace route or verify an honest route_unavailable response when no live bridge is configured.",
      "iterations": 2,
      "verifier": "route_available_or_unavailable",
      "steps": [
        {
          "method": "tools/call",
          "params": {
            "name": "operate",
            "arguments": {
              "intent": "vscode.workspace.list",
              "idempotency_key": "bench-vscode-ws-001"
            }
          }
        }
      ]
    },
    {
      "task_id": "v5.adapter.resolve_video_timeline_list",
      "goal": "Exercise the DaVinci Resolve read-only timeline route or verify an honest route_unavailable response when Resolve is not configured.",
      "iterations": 2,
      "verifier": "route_available_or_unavailable",
      "steps": [
        {
          "method": "tools/call",
          "params": {
            "name": "operate",
            "arguments": {
              "intent": "video.timeline.list",
              "idempotency_key": "bench-resolve-tl-001"
            }
          }
        }
      ]
    },
    {
      "task_id": "v5.adapter.capabilities_all",
      "goal": "Request the public Comptrol capabilities tool and verify a non-empty capability catalog.",
      "iterations": 1,
      "verifier": "capabilities_non_empty",
      "steps": [
        {
          "method": "tools/call",
          "params": {
            "name": "capabilities",
            "arguments": {}
          }
        }
      ]
    }
  ],
  "final_verifier": "all representative route verifiers pass; scripts/v5_adapter_catalog.py separately verifies all 14 runtime adapter descriptors"
}
